#include <bits/stdc++.h>
using namespace std;

template <typename T>
void f(ParamType param);

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    f(expr);

    return 0;
}